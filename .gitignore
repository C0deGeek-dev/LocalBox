# Working notes — kept locally, not pushed
plan-next.md
plan.md
analysis.md

# Per-machine settings overlay — overrides scalars in llm-models.json,
# never committed (paths are user-specific).
local-llm/settings.json
**/settings.local.json

# Runtime state generated by the profile — never commit
local-llm/profile-versions/
local-llm/parser-versions/
local-llm/bench-history.jsonl
local-llm/logs/
local-llm/tuner/
local-llm/localbench/
local-llm/tools/
local-llm/llama-cpp/
local-llm/llama-cpp-turboquant/
local-llm/gguf/
**/.llm-default

# Model weights / large local runtime artifacts
*.gguf
*.safetensors
*.onnx
*.pt
*.pth
*.ckpt
*.bin
*.st

# Backups / temp
*.bak
*.tmp
*.swp

# Python bytecode (proxy script)
__pycache__/
*.pyc

# .NET build output
**/bin/
**/obj/
**/publish/

# Windows / shell junk
Thumbs.db
desktop.ini
$RECYCLE.BIN/

.idea
*.backup

# Log files (dev / runtime temp logs)
*.log
/.localpilot.toml

# LocalMind / LocalPilot runtime stores (derived; never commit — ADR-0011)
.localmind/
.localpilot/
.localmind.toml
